# 法考知识库 使用指南（新人须知）

> 这份文档是给新加入的同学/使用者的入门说明。读完就能上手使用，也可让 AI 随时对照讲解。

## 一、这个知识库是什么？

「法考知识库」是一个辅助**国家统一法律职业资格考试**学习的个人知识库。它把散落的名师讲义、真题、笔记统一整理成结构化 Markdown，并配合前端页面，帮助你「学、练、回顾」。

它围绕 **3 大核心能力**：

| 能力 | 说明 | 在哪体验 |
|------|------|---------|
| ① 知识问答 | 在对话中提问，AI 检索知识库后回答，并标注引用的知识点 | 对话 |
| ② 每日十题 | 每天 10 个不同领域的法考题目，答案默认隐藏、点击展开 | 前端「每日十题」页 |
| ③ 知识点梳理 | 8 大部门法的核心考点结构化管理、浏览 | 前端「知识点」页 |

> 所有内容都是 **Markdown（.md）** 纯文本，方便阅读和 Git 版本管理，永远可追溯。

---

## 二、三种使用方法

### 方法 1：对话问答（最常用）

直接在对话里提问即可，例如：

> 问：「简述表见代理的构成要件」
> AI 会检索知识库，给出答案并标注引用，例如：
> `[知识点：表见代理的构成要件](knowledge/01_民法/01_总则编/表见代理.md)`

- **回答规则**：AI 会先检索 `knowledge/` 下的内容再回答，不凭记忆泛答。
- **引用标注**：答案旁会标注它引用的具体知识点文件，方便你回查原文。
- **法条准确性**：涉及法条会注明条文号（如《民法典》第 172 条）。
- **知识缺口**：如果知识库还没收录某个内容，AI 会如实说「知识库暂无该内容」，并把缺失点登记为「待补充」，下轮补齐。

### 方法 2：前端页面浏览

启动前端（见下方「四、快速启动」），打开浏览器访问即可。共有 3 个页面：

1. **📚 知识点页**——左侧是「部门法导航」，点开某个部门法可以看到它的核心考点文件和知识点文件；右侧展示 Markdown 内容（含加粗、表格、引用等）。
2. **📝 每日十题页**——左侧按日期列出每日题库，右侧是题目正文。每题答案放在 `<details>` 折叠块里，**点击才展开**，适合自测。
3. **💬 对话记录页**——按日期浏览历史所有问题、涉及的知识点、要点总结。

### 方法 3：提供资料入库

如果你有新的讲义、真题等核心资料，两种方式提交：

- **放到 `raw/` 文件夹**：把资料放进 `raw/`，告诉我一声，我提炼整理后写入 `knowledge/` 对应部门法目录。原始文件保留在 `raw/` 备查，**不会上传 git**。
- **对话直接提供**：直接把文字/图片内容发给我，我同样提炼整理入库。

整理后的入库位置和状态，都会记在 `agent.md` 的「原始资料整理记录」表里，可随时查看。

---

## 三、知识库结构速览

```
law-agent-workspace/
├── raw/               # 待整理的原始资料（不纳入 git）
├── knowledge/         # ★核心知识库：8 大部门法 Markdown
│   ├── 00_总纲/       # 考试框架、分值分布、备考策略
│   ├── 01_民法/       ├── 02_刑法/
│   ├── 03_民诉/       ├── 04_刑诉/
│   ├── 05_行政法/     ├── 06_商经知/
│   ├── 07_理论法/     └── 08_三国法/
├── questions/daily/   # 每日十题（YYYY-MM-DD.md）
├── conversations/     # 对话记录（问题 + 知识点 + 要点）
├── frontend/          # React + Vite 前端（npm run dev）
├── scripts/           # Markdown → JSON 构建脚本
├── README.md          # 项目简介
├── USAGE.md           # ★本使用指南
└── agent.md           # AI 工作规范
```

- 每个部门法目录下都有一个 **`00_核心考点总览.md`**，列出该科的分值、考频和高频考点，建议优先看它建立框架。
- 知识点文件命名如 `表见代理.md`，每篇通常包含：概念、构成要件、法条依据、易错点/口诀、真题提示。

---

## 四、快速启动（前端）

```bash
# 1. 安装依赖（首次）
cd frontend
npm install

# 2. 启动开发预览
npm run dev        # 打开 http://localhost:5173

# 3. 数据变更后重新构建（知识库/题目/记录改动后）
node scripts/build-data.js    # 在项目根目录执行
```

> 提示：如果浏览器打不开，可把地址改成 `http://127.0.0.1:5173/`。

---

## 五、常见问题 FAQ

**Q1：前端打开了但内容空白？**
先确认已在项目根目录跑过 `node scripts/build-data.js`，并确认 `frontend/public/data/*.json` 已生成。

**Q2：raw 里的资料会上传到 git 吗？**
不会。`raw/` 已在 `.gitignore` 中排除，用于存放大体积原始 PDF，避免仓库膨胀。

**Q3：想批量补充某个部门法的知识点怎么办？**
把对应讲义放进 `raw/` 或直接发我，我会沿「提炼 → 写入对应部门法 → 更新核心考点总览」的流程整理入库。

**Q4：AI 回答没有引用或答错了怎么办？**
提醒我「先检索知识库再回答、标注引用」，或告知具体缺失内容，我会登记为待补充并下一轮补齐。

---

## 六、给 AI 的提示（讲解时对照本文件）

当新人询问「知识库怎么用」时，可按本文件结构讲解：
1. **是什么** → 3 大能力
2. **怎么用** → 对话问答 / 前端浏览 / 资料入库 三方法
3. **结构长啥样** → 8 大部门法 + 每科总览
4. **怎么启动** → npm 命令
5. **常见坑** → FAQ
